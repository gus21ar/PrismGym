<UserControl x:Class="FAPrismGym.Module.Menu.Views.PUCMenuInicial"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True"
             >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Button Content="Clientes" Style="{DynamicResource BSPrincipal}" Grid.Row="0" Grid.Column="0" Command="{Binding ClientesCommand}"/>
        <Button Content="Asistencias" Style="{DynamicResource BSPrincipal}" Grid.Row="0" Grid.Column="1" Command="{Binding AsistenciasCommand}"/>
        <Button Content="Pagos" Style="{DynamicResource BSPrincipal}" Grid.Row="1" Grid.Column="0" Command="{Binding PagosCommand}"/>
        <Button Content="Reportes" Style="{DynamicResource BSPrincipal}" Grid.Row="1" Grid.Column="1" Command="{Binding ReportesCommand}"/>
    </Grid>
</UserControl>
